<h1 class="text-center">Create a new location</h1>
<hr>
<%= render "/shared/error_messages" %>
<div class="card">
    <div class="card-body">
        <%= form_with scope: :location, local: true, url: locations_path do |form| %>
        <div class="form-group">
            <%= form.label :name %>
            <%= form.text_field :name, class: 'form-control' %>
        </div>
        <button class="btn btn-success btn-block">Create new location</button>
        <% end %>
    </div>
</div>